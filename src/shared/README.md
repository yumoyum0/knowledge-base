# Shared Types

IPC channel constants, TypeScript interfaces, and shared type definitions.

**Target**: `types.ts` with all IPC channel names and API type signatures.

Convention: Channels defined once here, imported by both main and preload.
